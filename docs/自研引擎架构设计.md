# ScienceAndTheology — 自研引擎架构设计

> **本文档是 P0 阶段产出，是后续所有改造的共识基线。**
> 任何与本文档冲突的实现都应先更新本文档再实施。
> 历史背景见 [项目架构总览.md](项目架构总览.md) 与 [专用引擎性能优化方向.md](专用引擎性能优化方向.md)。

## 1. 背景与决策

### 1.1 为什么放弃 Godot

Voxel 游戏的性能瓶颈与 Godot 的设计哲学存在结构性冲突：

| Voxel 游戏需要 | Godot 提供 | 冲突 |
|---|---|---|
| 海量小数据（百万级 cell） | Node 树、Object 体系 | Node 是重量级对象，每个都带 _get_property、信号、notif |
| 1 draw call / chunk | MeshInstance3D + 多材质拆分 | 默认多 draw call |
| 自定义碰撞（chunk 级） | Bullet 物理 broadphase | 25 个 StaticBody 仍会被遍历 |
| 主线程零阻塞 | 主线程同步 _process | GDScript 主循环跑不掉 |
| GPU 直接上传 | ArrayMesh → RenderingServer 中转 | 多一层拷贝 |

C++ GDExtension 只能下沉"算 mesh 数据"，无法下沉"提交 mesh 给 GPU 和维护场景树"。
而 Java 版 MC 的快恰恰快在渲染提交层 — 它根本没有场景图，直接 glDraw。
继续在 Godot 体系内优化天花板有限，永远会比 Java MC 卡。

### 1.2 决策记录

经过 6 种架构方案对比（完全自研 / Godot 当壳 / Fork Godot / 移植 Godot 模块 / 换引擎 / 进程分离），
选定方案 A：**完全自研引擎**。

| 决策项 | 选择 | 理由 |
|---|---|---|
| 主架构 | 完全自研引擎 | 从根本上消除 Godot 场景图开销 |
| 渲染 API | 仅 Vulkan | 一套代码覆盖 Win/Linux/Android；macOS 走 MoltenVK |
| 窗口/输入 | SDL3 | 跨平台，输入/音频/窗口一体 |
| UI 系统 | 完整复刻 MUI（Modern UI） | MVVM + 保留模式 + SDF 文本 + HarfBuzz + emoji |
| 数据架构 | 纯 ECS（Entity-Component-System） | 海量机器/生物/掉落物的数据连续布局 + 可并行 tick |
| ECS 内核 | EnTT (header-only, MIT) | C++ ECS 事实标准，sparse set 存储 |
| 脚本系统 | 分层：C++（引擎核心）+ AngelScript（玩法脚本） | C++ 语法风格、JIT 编译、<50ms 热重载、专为游戏设计 |
| 物理引擎 | 自研 voxel 碰撞查询 | 针对 voxel 优化，无 broadphase 负担 |
| 音频 | miniaudio | 单文件头，跨平台，0 依赖 |
| 资产格式 | JSON + 二进制 raw | JSON 可读，二进制高效 |
| 构建系统 | CMake | 现有项目已用，DLL 模块化构建 |
| 编程语言 | C++20（引擎核心）+ AngelScript（玩法脚本） | 引擎类型安全 + 性能极致；脚本 C++ 语法 0 学习成本 + 真热重载 |
| 仓库策略 | 同仓库新增 `engine/` 目录 | 保留 git 历史，渐进迁移 |
| Godot 移除时机 | P1 开始立即移除 | 避免双轨维护 |

### 1.2.1 脚本系统选型理由

经过 7 种脚本方案对比（C++ DLL 热重载 / Lua+sol2 / C# NativeAOT / AngelScript / QuickJS / LLVM JIT Cling / Python），
选定 **分层方案：C++（引擎核心）+ AngelScript（玩法脚本）**：

**为什么放弃纯 C++ DLL 热重载：**
- **迭代太慢**：DLL 编译+链接 5-30 秒，违背脚本快速迭代初衷
- **ABI 约束**：即使同编译器，STL 跨边界、异常跨边界、shared_ptr 所有权等问题严峻
- **DLL 崩带走进程**：脚本错误直接崩溃引擎，无隔离

**为什么选 AngelScript：**
- **C++ 语法 0 学习成本**：语法 ~90% 等同 C++（class、template、interface、namespace 都有）
- **真热重载**：.as 文件保存即重载，<50ms 生效（JIT 编译）
- **性能中等偏上**：比 Lua 快 2-5 倍（有 JIT），适合游戏逻辑
- **专为游戏设计**：垃圾回收、协程、模块系统、调试器支持
- **体积小**：~2MB 运行时
- **Windows 支持良好**：MSVC 编译，无跨平台问题

**分层策略：**
- **C++ 层**（引擎核心）：渲染、物理、ECS 内核、IO、音频 — 性能敏感
- **AngelScript 层**（玩法脚本）：配方、任务、机器控制流、UI ViewModel — 快速迭代
- **Binding 层**：手动注册（`engine->RegisterObjectMethod(...)`），不自动生成

**代价：**
- 需要维护 C++ ↔ AngelScript binding 层（手动注册）
- 性能敏感逻辑不能用 AngelScript 写（要下沉到 C++）

### 1.2.2 ECS 选型理由

经过 4 种数据架构方案对比（纯 OOP / 纯 ECS / 混合架构 / 轻量 ECS），选定 **纯 ECS**：

- **工厂自动化场景**：GregTech 风格机器数量轻松过 1000，必须数据连续布局
- **并行 tick**：不同 System 可并行跑（机器/生物/网络独立）
- **扩展性**：加新能力 = 加新 Component，不改现有代码
- **与 AngelScript 兼容**：脚本通过 Binding 层 API 访问 Component，不直接碰内存
- **EnTT 内核**：header-only，sparse set 存储，C++ ECS 事实标准

### 1.3 代价承认

| 维度 | 代价 |
|---|---|
| 现有 GDScript 脚本 | 30+ 个全部作废重写为 AngelScript |
| 现有 .tscn 场景 | 全部作废 |
| 现有 .tres 资源 | 改为 JSON 格式重新组织 |
| 现有 C++ 代码 | ~80% 算法层可复用，需剥离 godot-cpp 依赖；Manager 类要重构为 ECS System |
| Godot 编辑器 | 失去，调坐标靠日志/ImGui |
| 回到现状功能 | 3-6 个月（全职） |

## 2. 设计原则

1. **数据驱动**：所有配置走 JSON，玩法逻辑走 AngelScript，引擎核心是 C++ ECS。
2. **零主线程阻塞**：meshing、IO、布局全在 worker 线程，主线程只做提交。
3. **零场景图**：不维护 Node 树，渲染对象用 RID 表直接管理。
4. **数据导向（DOD）**：POD Component + sparse set 存储，cache 友好。
5. **MVVM 分层**：UI 严格分 View / ViewModel / Model，数据绑定驱动。
6. **算法优先于抽象**：hot path 保持 plain struct + 索引数组。
7. **可演进为无头服务端**：核心系统不依赖窗口、相机、UI。
8. **跨平台预留**：Vulkan + SDL3 + miniaudio 已覆盖主流平台；macOS 走 MoltenVK。
9. **分层语言栈**：C++ 引擎核心（性能敏感）+ AngelScript 玩法脚本（快速迭代），binding 层手动注册。

## 3. 系统分层

```text
┌──────────────────────────────────────────────────────────────┐
│ 应用层 Application                                            │
│ AngelScript (.as → JIT)：玩法逻辑、UI ViewModel、              │
│   配方、任务、机器控制流、自定义 Component/System             │
│ engine_app/：游戏主循环、场景切换、存档入口                   │
├──────────────────────────────────────────────────────────────┤
│ 引擎层 Engine                                                 │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ ECS 世界 (EnTT)                                         │  │
│ │ Entity / Component (sparse set) / System / Scheduler   │  │
│ │ 编译期 Component + 运行时 Component (脚本注册)          │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ UI 系统 (复刻 MUI)                                      │  │
│ │ View (保留模式) / ViewModel (数据绑定) / Text Engine    │  │
│ │ Arc 2D 渲染器 / SDF 字体 / HarfBuzz / Emoji / Animation│  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ Voxel 渲染系统                                          │  │
│ │ ChunkRenderer (Vulkan) / Greedy Mesh / LOD / Streaming │  │
│ │ Worker 线程池 / VertexBuffer 池                        │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ Voxel 物理系统                                          │  │
│ │ 自研 voxel 碰撞查询 / 玩家 AABB / 射线检测             │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ 数据系统                                                │  │
│ │ WorldData / TerrainGenerator / SaveManager / Networks  │  │
│ │ TickSystem / Ecosystem / DayNight                      │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ 脚本系统 (AngelScript)                                 │  │
│ │ ScriptManager / ScriptEngine / FileWatcher             │  │
│ │ Binding Layer (手动注册 C++ API → AS)                  │  │
│ │ Registry Hub (recipes/machines/quests/events)          │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ 资产系统                                                │  │
│ │ AssetLoader (JSON + 二进制 raw) / TextureCache         │  │
│ │ / FontAtlas / ShaderCache                              │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ 音频系统                                                │  │
│ │ miniaudio 封装 / 3D 音频 / 音轨池                       │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ 网络系统                                                │  │
│ │ SteamP2P (复用) / Packet / Replication                 │  │
│ └────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│ 渲染后端 Render Backend                                       │
│ Vulkan 1.3 / MoltenVK (macOS) / VMA 内存管理 / SPIR-V        │
├──────────────────────────────────────────────────────────────┤
│ 平台层 Platform                                               │
│ SDL3 (窗口/输入/事件) / std::filesystem / 线程库             │
└──────────────────────────────────────────────────────────────┘
```

## 4. 模块依赖图

```text
                    ┌──────────────┐
                    │  engine_app  │ ── AngelScript 入口、游戏主循环
                    └──────┬───────┘
                           │
        ┌──────────┬───────┼────────┬──────────┐
        ▼          ▼       ▼        ▼          ▼
   ┌─────────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌────────┐
   │ ui_mui  │ │voxel │ │voxel │ │script│ │ audio  │
   │         │ │render│ │physic│ │(C++) │ │        │
   └────┬────┘ └──┬───┘ └──┬───┘ └──┬───┘ └───┬────┘
        │         │        │        │         │
        └─────────┴────────┴────────┴─────────┘
                          │
                  ┌───────┴────────┐
                  │   ecs_world    │ ── EnTT Entity/Component/System
                  └───────┬────────┘
                          │
                  ┌───────┴────────┐
                  │   data_core    │ ── WorldData / Terrain / Save / Networks
                  └───────┬────────┘
                          │
                  ┌───────┴────────┐
                  │ render_backend │ ── Vulkan / VMA / SPIR-V
                  └───────┬────────┘
                          │
                  ┌───────┴────────┐
                  │   platform     │ ── SDL3 / filesystem / threads
                  └────────────────┘
```

依赖规则：
- 上层可依赖下层，反之禁止。
- 同层模块之间通过事件总线（EventBus）通信，禁止直接引用。
- `ecs_world` 是核心调度枢纽，所有 System 通过它访问 Component。
- `data_core` 独立于渲染后端，可单独编译为无头服务端。
- `script` 通过 ECS API 访问 Component，不直接碰内存。

## 5. 核心子系统设计

### 5.1 Voxel 渲染系统

**目标**：1 draw call / chunk，主线程零阻塞，对齐 Java MC（Sodium 风格）。

```text
Main Thread                          Worker Thread Pool
─────────────                        ──────────────────
1. 排序 chunk by 距离                1. 取 chunk terrain 数据
2. frustum cull (native 数组)        2. 跑 greedy mesh (复用现有算法)
3. for each visible chunk:           3. 写入 staging VertexBuffer
      vkCmdBindVertexBuffers         4. atomic 标记 ready
      vkCmdDrawIndexed               5. callback 通知主线程 upload
```

**关键设计**：
- chunk → RID 表：`unordered_map<ChunkCoord, ChunkRenderHandle>`
- 每个 ChunkRenderHandle 持有 vertex buffer / index buffer / instance transform
- 材质 atlas 化：所有方块纹理合到一张图集，UV2 区分
- LOD：near = full mesh + collision；mid = simplified mesh；far = billboard
- meshing 完全在 worker 线程，主线程只 `vkCmdBind` + `vkCmdDraw`

**复用现有代码**：
- `src/bindings/world/gd_chunk_helper.cpp::build_greedy_mesh` 算法逻辑保留
- `src/bindings/world/gd_chunk_helper.cpp::build_collision_faces` 算法逻辑保留
- `src/core/save/*` region_file / chunk_serializer 文件格式保留
- `src/core/simulation/*` 地形生成保留

### 5.2 Voxel 物理系统

**目标**：玩家走 voxel 碰撞，无 broadphase 负担。

- 玩家 AABB vs voxel grid：直接采样 `WorldData.get_cell(cell)`，O(N) N=玩家附近 cell 数
- 射线检测：DDA 算法遍历 voxel grid，O(射线路径长度)
- 刚体（掉落物、生物）：Jolt Physics（远期）；当前只有玩家和静态机器，自研够用
- 远距离 chunk 无碰撞（已有 `horizontal_lod0_radius` 概念）

### 5.3 UI 系统（完整复刻 MUI）

**参考**：[BloCamLimb/ModernUI](https://github.com/BloCamLimb/ModernUI)

#### 5.3.1 架构

```text
┌─────────────────────────────────────────────────────┐
│ View Layer (保留模式 UI)                             │
│ View 树：ViewGroup / TextView / Button / ImageView  │
│ 布局：Measure / Layout / Draw 三阶段                │
│ 输入：dispatchTouchEvent / onInterceptTouchEvent    │
│ 动画：ValueAnimator / Transition                    │
├─────────────────────────────────────────────────────┤
│ ViewModel Layer                                      │
│ ViewModel：持有 UI State，双向绑定到 View           │
│ LiveData / StateFlow：响应式数据流                  │
│ DataBinding：JSON 布局 + 表达式绑定                 │
├─────────────────────────────────────────────────────┤
│ Text Engine (复刻 MUI Modern Text Engine)           │
│ HarfBuzz 整形 / ICU 国际化 / BiDi 算法             │
│ SDF 字体渲染 / Emoji (Noto Color Emoji)            │
│ 异步布局 / 线程安全布局缓存                         │
├─────────────────────────────────────────────────────┤
│ Render Engine (Arc 2D)                              │
│ Vulkan 2D 渲染管线 / Shader 圆角矩形/环形/扇形      │
│ 字体图集 / 矩形压缩算法 / Mipmap                    │
│ 多线程渲染命令录制                                  │
└─────────────────────────────────────────────────────┘
```

#### 5.3.2 完整能力清单（MUI 复刻范围）

**文本引擎**：
- Unicode 16.0 码点处理（U16→U32 转换、修复无效代理对）
- HarfBuzz 文本整形（连字体、上下文、变种选择符）
- ICU BiDi 算法（阿拉伯语、希伯来语等 RTL）
- Unicode 分行算法（语境与语言相关换行）
- 字素簇分断算法（Grapheme Cluster Break）
- 字体回退（自动分配字体、调整行高）
- TTF/OTF 字体支持，可配置基准大小、基线偏移、阴影
- SDF 距离场字体渲染（2D 和 3D 通用）
- 原生粗斜体渲染，自动切换分辨率等级（最高 96 号）
- FreeType 字体微调 + Mipmap + 屏幕空间 AA
- 像素网格自动对齐
- Alpha 遮罩图存储纹理（省显存）
- 矩形压缩算法实时生成字体图册
- 异步并发文本布局（布局与渲染分离，线程安全）
- 高速文本布局缓存，支持布局拼接
- Google Noto Color Emoji 15.1，Unicode 15.1 Emoji List
- CommonMark / HTML 语法支持，自定义嵌入内容
- 多区域设置支持

**View 系统**：
- 保留模式 View 树（类 Android，但独立实现）
- ViewGroup 容器：LinearLayout / FrameLayout / RelativeLayout / ConstraintLayout
- 基础控件：TextView / Button / ImageView / EditText / CheckBox / Slider / ProgressBar / ScrollView / ListView / GridView / Spinner
- 布局三阶段：Measure → Layout → Draw
- 输入事件：dispatchTouchEvent / onInterceptTouchEvent / onTouchEvent
- 焦点系统：requestFocus / clearFocus / nextFocus
- 键盘事件分发
- 滚动惯性 + 平滑滚动
- 拖拽支持

**ViewModel 与数据绑定**：
- ViewModel 持有 UI State（不可变快照）
- LiveData / StateFlow 响应式数据流
- 双向绑定：View ↔ ViewModel
- JSON 布局文件 + 表达式绑定（类似 Android DataBinding）
- 命令绑定（Button click → ViewModel method）

**动画**：
- ValueAnimator / ObjectAnimator
- 属性动画：alpha / translation / scale / rotation
- 转场动画：fade / slide / scale / shared element
- 状态机动画（StateListAnimator）
- 插值器：Linear / Accelerate / Decelerate / Anticipate / Overshoot / Bounce
- GPU Shader 直接绘制圆角矩形 / 环形 / 扇形（减少 CPU 计算）

**窗口与全局**：
- 多窗口模式（borderless fullscreen / maximized / windowed）
- 帧率限制 + 音量淡出（窗口失焦时）
- 屏幕背景高斯模糊
- 屏幕截图
- 偏好设置中心（类 Android Settings）

**国际化**：
- 多语言资源包
- 运行时切换语言
- 区域设置感知的数字 / 日期 / 货币格式化

#### 5.3.3 字体资产

- 默认字体：Noto Sans CJK（覆盖中英日韩）
- Emoji：Google Noto Color Emoji 15.1
- 等宽字体：JetBrains Mono（代码/调试用）
- 像素字体：可选，用于复古 UI 风格

### 5.4 资产系统

- 配置文件（worldgen_config / PlanetDescriptor / recipes / quests）：JSON
- 大块数据（chunk save / region / 贴图 raw）：二进制
- 贴图加载：stb_image 单文件头
- 字体加载：FreeType + HarfBuzz
- Shader：GLSL → SPIR-V（shaderc 编译期编译）
- 资产清单：`assets/manifest.json` 列出所有资产及其哈希

### 5.5 脚本系统（AngelScript）

采用分层方案：C++ 引擎核心 + AngelScript 玩法脚本。脚本为 .as 文件，JIT 编译后执行，支持热重载。

#### 5.5.1 架构

```text
┌──────────────────────────────────────────────────────────────┐
│ ScriptManager (引擎核心, 单例, 主线程)                       │
│  • 编译/加载 .as 脚本模块                                    │
│  • File watcher (worker 线程, 监视 .as 文件变化)              │
│  • Reload 队列 (主线程消费, 避免竞态)                         │
│  • Script ID 表 (脚本唯一标识)                                │
├──────────────────────────────────────────────────────────────┤
│ Binding Layer (C++ → AngelScript, 手动注册)                  │
│  • 注册基础类型 (int, float, string, bool)                   │
│  • 注册数学类型 (Vector3, Quaternion, Matrix)                │
│  • 注册 ECS 接口 (World, Entity, Transform, MeshRef)         │
│  • 注册 Input / Asset / Logger 接口                          │
├──────────────────────────────────────────────────────────────┤
│ Registry Hub (各子系统注册表, 主线程)                         │
│  • RecipeRegistry (scripted/backup 双列表)                   │
│  • MachineRegistry / QuestRegistry / BlockRegistry           │
│  • EventRegistry (script_id + callback_id 间接派发)          │
│  • StateStore (跨 reload 持久状态)                            │
├──────────────────────────────────────────────────────────────┤
│ AngelScript Engine (asIScriptEngine)                          │
│  • 模块编译 (asIScriptModule)                                │
│  • 上下文执行 (asIScriptContext)                             │
│  • 垃圾回收 (asIScriptEngine::GarbageCollect)               │
│  • 调试器接口 (asIDebugger)                                  │
└──────────────────────────────────────────────────────────────┘
```

#### 5.5.2 脚本入口

```angelscript
// scripts/game/machines/bronze_furnace.as

#include "snt/api.as"

// Script entry: called on load and on every reload.
void snt_entry() {
    snt::log::info("Loading Bronze Furnace mod...");

    // L2: register item + block
    snt::items::register_item({
        id = "bronze_furnace",
        display_name = "Bronze Furnace",
        texture = "items/bronze_furnace.png"
    });

    // L1: recipes
    snt::recipes::add("bronze_furnace", {
        input = "copper_ore",
        outputs = {{"copper_ingot", 1}, {"slag", 1}},
        duration = 150,
        eu_per_tick = 3
    });

    // L3: register custom System
    snt::systems::register_system({
        name = "bronze_furnace_tick",
        required_components = {"MachineComponent", "BronzeFurnaceState"},
        tick_rate = 20.0f,
        can_run_on_worker = true,
        callback_id = "on_bronze_furnace_tick"
    });
}
```

#### 5.5.3 完整 API 清单（参考 GroovyScript）

以下 API 通过 C++ Binding 层注册到 AngelScript 全局命名空间 `snt::`：

```angelscript
// Recipe System (参考 GS recipes.add/remove)
namespace snt::recipes {
    struct Output { string item; int count = 1; };
    struct RecipeDef {
        string input;
        array<Output> outputs;
        int duration = 200;
        int eu_per_tick = 4;
        string? tag;
    };
    struct RecipeFilter {
        string? input;
        string? output;
        string? tag;
    };
    void add(const string&in machine, RecipeDef def);
    void remove(const string&in machine, RecipeFilter filter);
    array<RecipeDef> list(const string&in machine);
}

// Material Registration
namespace snt::materials {
    struct MaterialDef {
        string id;
        string display_name;
        uint32 color;
        array<string> forms;
        int? blast_temp;
    };
    void add(MaterialDef def);
}

// Item / Block Registration
namespace snt::items {
    struct ItemDef {
        string id, display_name, texture;
        int max_stack = 64;
    };
    void register_item(ItemDef def);
}
namespace snt::blocks {
    struct BlockDef {
        string id, display_name, texture_all;
        float hardness = 1.0f;
        bool collision = true;
        string on_place, on_break, on_interact;
    };
    void register_block(BlockDef def);
}

// Machine Registration
namespace snt::machines {
    struct MachineDef {
        string id, display_name;
        int tier = 1, power_capacity = 0;
        array<string> recipe_types;
    };
    void register_machine(MachineDef def);
}

// Quest System
namespace snt::quests {
    struct QuestDef {
        string id, title, description;
        array<string> prerequisites, objectives, rewards;
    };
    void add(QuestDef def);
    void progress(EntityRef player, const string&in objective_id, int amount = 1);
}

// Event Bus (参考 GS eventManager.listen)
namespace snt::events {
    // Listen by name + callback_id.
    // Internally stores (script_id, callback_id) — NOT a function pointer.
    void listen(const string&in event_name, const string&in callback_id);
}

// Packmode (参考 GS packmode)
namespace snt::packmode {
    enum Mode { NORMAL, EXPERT };
    Mode current();
    bool is_normal();
    bool is_expert();
}

// Persistent State (跨 reload 不丢失)
namespace snt::state {
    void set(const string&in key, const string&in value);
    string? get(const string&in key);
    void set_int(const string&in key, int64 v);
    int64? get_int(const string&in key);
}

// Logging
namespace snt::log {
    void info(const string&in msg);
    void warn(const string&in msg);
    void error(const string&in msg);
}

// Info Commands (参考 GS /gs hand)
namespace snt::info {
    string hand_item_script();
    string looking_block_script();
}
```

#### 5.5.4 Reload 机制（参考 GS 双列表状态管理）

AngelScript 热重载比 DLL 简单很多（无 ABI 问题、无符号冲突），但仍需处理状态迁移：

| 坑 | 方案 |
|---|---|
| 指针/引用失效 | 事件存 `(script_id, callback_id)`，不存函数指针 |
| 全局/静态变量丢失 | 脚本不持有跨 reload 状态，走 `snt::state` API |
| 内存所有权混乱 | 脚本传值（struct copy），不传 C++ 对象引用 |
| 模块依赖 | 编译时解析 `#include`，重载按依赖图拓扑排序 |
| GC 延迟 | 重载后主动调用一次 `GarbageCollect()` |

```cpp
// ScriptManager::reload_script_internal (C++ 侧)
void reload_script_internal(ScriptEntry& entry) {
    // 1. Call on_reload (script-defined cleanup, AS function)
    if (entry.reload_fn) call_as_function(entry.reload_fn);
    // 2. Registry cleanup: scripted list cleared, backup restored
    registry_hub.on_script_unload(entry.id);
    // 3. Recompile .as module (no DLL unload/reload needed!)
    entry.module = compile_as_module(entry.path);
    // 4. Re-call entry to re-register
    entry.entry_fn = get_as_function(entry.module, "snt_entry");
    call_as_function(entry.entry_fn);
    // 5. Proactive GC pass
    as_engine->GarbageCollect();
}
```

#### 5.5.5 命令系统（参考 GS）

```text
/snt reload           — reload all scripts
/snt reload <name>    — reload one script
/snt hand             — print hand item in script format
/snt looking          — print looking block in script format
/snt list recipes     — list all recipes
/snt list scripts     — list loaded scripts
/snt packmode normal  — switch to normal
/snt packmode expert  — switch to expert (triggers full reload)
/snt wiki             — generate wiki files
```

#### 5.5.6 脚本能力层级（L1-L3）

| 层级 | 能力 | API |
|---|---|---|
| L1 数据 | 配方 / 材料 / 任务 | `recipes.add` / `materials.add` / `quests.add` |
| L2 注册 | 物品 / 方块 / 机器 / Component 类型 | `items.register_item` / `blocks.register_block` / `components.register_type` |
| L3 行为 | 自定义 System / 事件回调 / Entity 访问 | `systems.register_system` / `events.listen` / `entity.get_*` |

L4（引擎核心扩展），渲染/物理/IO 。

#### 5.5.7 脚本与 ECS 集成

详见 5.8 ECS 架构。脚本通过以下 API 访问 ECS：

- `snt::components::register_type` — 注册运行时 Component 类型
- `snt::systems::register_system` — 注册自定义 System（声明 Component 需求）
- `snt::events::listen` — 事件回调，引擎派发匹配的 Entity 列表
- `snt::entity::get_*` / `set_*` — 通过值类型访问 Component
- `snt::components::get_int` / `set_float` — 通过名字访问运行时 Component 字段

#### 5.5.8 脚本文件组织

AngelScript 脚本不需要编译为 DLL，直接放在 `scripts/` 目录，运行时由 ScriptManager 编译：

```text
scripts/
├── game/
│   ├── recipes/          # L1: 配方脚本
│   ├── machines/         # L2: 机器注册 + L3: 机器 tick 系统
│   ├── quests/           # L1: 任务定义
│   └── items/            # L2: 物品注册
├── shared/
│   └── api.as            # 引擎 API 声明（include 用）
└── mods/                 # Mod 脚本
    └── example/
        ├── manifest.json
        └── mod.as
```

引擎启动时 ScriptManager 扫描 `scripts/` 目录，编译所有 .as 文件为 AngelScript 模块。热重载时只需重新编译变化的文件。

#### 5.5.9 完整脚本示例

```angelscript
// scripts/game/machines/bronze_furnace.as
#include "snt/api.as"

// Script entry: called on load and on every reload.
void snt_entry() {
    snt::log::info("Loading Bronze Furnace mod...");

    // L2: register custom Component for machine state
    snt::components::register_type({
        name = "BronzeFurnaceState",
        fields = {
            {"temperature", snt::components::FieldType::FLOAT, "20.0"},
            {"fuel_ticks_remaining", snt::components::FieldType::INT, "0"}
        }
    });

    // L2: register item + block
    snt::items::register_item({
        id = "bronze_furnace",
        display_name = "Bronze Furnace",
        texture = "items/bronze_furnace.png"
    });
    snt::blocks::register_block({
        id = "bronze_furnace",
        display_name = "Bronze Furnace",
        texture_all = "blocks/bronze_furnace.png",
        hardness = 3.5f,
        on_interact = "on_bronze_furnace_interact"
    });

    // L1: recipes
    snt::recipes::add("bronze_furnace", {
        input = "copper_ore",
        outputs = {{"copper_ingot", 1}, {"slag", 1}},
        duration = 150, eu_per_tick = 3
    });

    // L3: register custom System
    snt::systems::register_system({
        name = "bronze_furnace_tick",
        required_components = {"MachineComponent", "BronzeFurnaceState"},
        tick_rate = 20.0f,
        can_run_on_worker = true,
        callback_id = "on_bronze_furnace_tick"
    });

    snt::events::listen("block.interact", "on_bronze_furnace_interact");
}

// Callback dispatcher: ScriptManager calls this by callback_id.
void snt_callback(const string&in callback_id, snt::events::SystemTickEvent@ evt) {
    if (callback_id == "on_bronze_furnace_tick") {
        for (uint i = 0; i < evt.entity_count; ++i) {
            snt::EntityId e = evt.entities[i];
            double temp = snt::components::get_float(e, "BronzeFurnaceState", "temperature");
            // tick logic...
            snt::components::set_float(e, "BronzeFurnaceState", "temperature", temp + 2.5);
        }
    }
}
```

#### 5.5.10 与 GroovyScript 对比

| GroovyScript | AngelScript |
|---|---|
| `.groovy` 文件 | `.cpp` 文件 → `.dll` |
| `recipes.add(...)` | `snt::recipes::add(...)` |
| `eventManager.listen(...)` | `snt::events::listen(..., callback_id)` |
| `packmode` | `snt::packmode::is_expert()` |
| `/gs reload` | `/snt reload` |
| `GroovyReloadEvent` | `SNT_SCRIPT_RELOAD()` |
| Groovy class cache | AS module + RegistryHub 双列表 |
| `scripted` / `backup` 列表 | `RecipeRegistry` 的 `_live` / `_backup` |
| 不能加新方块/物品 | ✓ 可注册新方块/物品/机器/Component/System |

### 5.6 音频系统

- miniaudio 单文件头
- 3D 音频：手动计算 pan/volume（距离衰减 + HRTF 简化版）
- 音轨池：BGM / SFX / UI 三组独立音量
- 格式：WAV / OGG / FLAC（miniaudio 自带解码）

### 5.7 网络/多人系统

- 复用现有 `GodotSteamP2PAdapter` 的协议层逻辑，剥离 Godot 依赖
- Steamworks SDK 独立于 Godot，可直接复用
- Packet 序列化：自定义二进制协议（msgpack 备选）
- Replication：服务端权威 + 客户端预测（已有 `prediction_buffer`）

### 5.8 ECS 架构

采用纯 ECS（Entity-Component-System）作为引擎核心数据架构。基于 EnTT（header-only，MIT 协议）。

#### 5.8.1 核心类型

```cpp
// snt_engine/ecs/types.h（未实现，直接用 entt::entity）
namespace snt::ecs {

// Entity = ID + version (防止 use-after-free)
using EntityId = uint64_t;
constexpr EntityId INVALID_ENTITY = 0;

struct Entity {
    EntityId id;
    uint32_t version;  // 每次复用 id 时 +1
    bool is_valid() const { return id != INVALID_ENTITY; }
};

} // namespace snt::ecs
```

#### 5.8.2 Component 存储策略：Sparse Set

选 Sparse Set 而非 Archetype（Unity DOTS 那种），理由：
- 加/删 Component O(1)
- 支持运行时动态 Component（脚本注册需要）
- 现成成熟库：EnTT

```cpp
// snt_engine/ecs/world.h
namespace snt::ecs {

// World wraps entt::registry, adds engine-specific helpers.
class World {
public:
    // Entity lifecycle
    Entity create_entity();
    void destroy_entity(Entity e);
    bool is_alive(Entity e) const;

    // Component ops (compile-time known types, fast path)
    template<typename T>
    void add_component(Entity e, T&& comp);
    template<typename T>
    T* get_component(Entity e);
    template<typename T>
    bool has_component(Entity e) const;
    template<typename T>
    void remove_component(Entity e);

    // Component ops (runtime-registered types, script path)
    void add_component(Entity e, ComponentTypeId tid, const void* data);
    void* get_component(Entity e, ComponentTypeId tid);
    void remove_component(Entity e, ComponentTypeId tid);

    // Query: returns all entities matching a component signature
    template<typename... Components>
    auto view();

    // System registration
    void register_system(std::unique_ptr<ISystem> sys);
    void update_systems(float delta);

private:
    entt::registry _registry;
    std::vector<std::unique_ptr<ISystem>> _systems;
    std::unordered_map<ComponentTypeId, std::unique_ptr<ComponentStorageBase>> _runtime_storages;
};

} // namespace snt::ecs
```

#### 5.8.3 System 接口

```cpp
// snt_engine/ecs/system.h
namespace snt::ecs {

class ISystem {
public:
    virtual ~ISystem() = default;
    virtual void update(World& world, float delta) = 0;

    // Scheduling hints
    virtual int priority() const { return 0; }            // lower = earlier
    virtual bool can_run_on_worker() const { return false; }
    virtual float tick_rate() const { return 60.0f; }     // Hz
    virtual std::string_view name() const = 0;
};

// Example: machine tick system
class MachineTickSystem : public ISystem {
public:
    std::string_view name() const override { return "MachineTickSystem"; }
    float tick_rate() const override { return 20.0f; }
    bool can_run_on_worker() const override { return true; }

    void update(World& world, float delta) override {
        auto view = world.view<PositionComponent, MachineComponent,
                               InventoryComponent, PowerComponent>();
        for (auto entity : view) {
            auto& machine = world.get_component<MachineComponent>(entity);
            // tick logic...
        }
    }
};

} // namespace snt::ecs
```

#### 5.8.4 标准 Component 定义

| Component | 字段 | 用途 |
|---|---|---|
| `PositionComponent` | x, y, z, chunk_x/y/z | 实体位置 |
| `VelocityComponent` | vx, vy, vz | 速度 |
| `HealthComponent` | current, max, regen_per_sec | 生命值 |
| `InventoryComponent` | slots[], max_slots | 物品栏 |
| `MachineComponent` | machine_type, current_recipe_id, progress, state | 机器状态 |
| `PowerComponent` | energy_stored, energy_capacity, voltage_tier | 电力 |
| `RenderHandleComponent` | mesh_rid, instance_rid, visible, lod_level | 渲染句柄 |
| `CollisionHandleComponent` | collision_rid, has_collision | 碰撞句柄 |
| `ChunkStateComponent` | state, player_distance_sq | chunk 状态 |
| `TickRateComponent` | tick_rate, accumulator | tick 频率 |
| `InputStateComponent` | move_x/y, look_pitch/yaw, jump/sprint/interact | 输入 |
| `CameraRefComponent` | camera_entity, first_person | 相机 |

#### 5.8.5 Entity 分类

| Entity 类型 | 必备 Component | 数量级 |
|---|---|---|
| ChunkEntity | Position, TerrainData, RenderHandle, CollisionHandle, ChunkState | 海量 |
| MachineEntity | Position, MachineType, Inventory, Power, TickRate | 1000+ |
| CreatureEntity | Position, Velocity, Health, AIState, Inventory | 100+ |
| DroppedItemEntity | Position, Velocity, ItemData, Lifetime | 100+ |
| PlayerEntity | Position, Velocity, Health, Inventory, InputState, CameraRef | 1-few |
| ProjectileEntity | Position, Velocity, Damage, Lifetime, OwnerEntity | variable |
| NetworkNodeEntity | Position, NetworkType, PowerGrid/FluidGrid/ItemGrid | 100+ |
| StructureEntity | Position, StructureDef, MultiblockState, Children | variable |

#### 5.8.6 System 调度分组

```text
Main Thread 60 Hz:
  - InputSystem          (collect input → InputStateComponent)
  - PlayerMovementSystem (apply velocity, collision)
  - CameraSystem         (follow player)
  - RenderSubmitSystem   (submit draw calls to Vulkan)
  - UISystem             (draw MUI)
  - ChunkLODSystem       (update chunk visibility)

Worker Pool 20 Hz (parallel):
  - MachineTickSystem       (1000+ machines)
  - RecipeProcessSystem     (consumes inputs, produces outputs)
  - PowerNetworkSystem      (resolve power flow)
  - FluidNetworkSystem
  - ItemPipeSystem
  - CreatureAISystem

Worker Pool 10 Hz (parallel):
  - CreatureSpawnSystem
  - DayNightSystem
  - EcosystemTickSystem

Async (event-driven):
  - ChunkStreamingSystem    (load/unload chunks)
  - ChunkMeshingSystem      (greedy mesh on worker)
  - SaveSystem              (region file IO)
  - NetworkReplicationSystem
```

```cpp
// engine/ne/nn/ee/engine/ecs/system_sc
namespace snt::ecs {

class SystemScheduler {
public:
    struct ScheduleGroup {
        float tick_rate;       // 60, 20, async
        bool on_worker;        // main thread or worker pool
        std::vector<ISystem*> systems;
    };

    void schedule(std::unique_ptr<ISystem> sys);
    void run_main_thread(float delta);
    void run_worker_pool(float delta);
    void run_async(float delta);

private:
    std::vector<ScheduleGroup> _groups;
    WorkerThreadPool _workers;
};

} // namespace snt::ecs
```

#### 5.8.7 ECS + AngelScript 集成

脚本通过以下方式访问 ECS，**不直接碰 Component 内存**：

1. **注册运行时 Component**（脚本自定义状态）：
   ```cpp
   snt::components::register_type({
       .name = "BronzeFurnaceState",
       .fields = {{"temperature", FieldType::FLOAT, "20.0"}}
   });
   ```

2. **注册自定义 System**（声明 Component 需求）：
   ```cpp
   snt::systems::register_system({
       .name = "bronze_furnace_tick",
       .required_components = {"MachineComponent", "BronzeFurnaceState"},
       .tick_rate = 20.0f,
       .callback_id = "on_bronze_furnace_tick"
   });
   ```

3. **事件派发**：引擎查询匹配 Entity，通过 `SystemTickEvent` 派发给脚本 callback：
   ```cpp
   struct SystemTickEvent {
       std::string system_name;
       float delta;
       uint64_t entity_count;
       EntityId* entities;        // array of matching entities
   };
   ```

4. **Component 访问**（值类型，安全）：
   ```cpp
   // 标准 Component（typed，快路径）
   PositionComponent pos = snt::entity::get_position(e);
   snt::entity::set_position(e, pos);

   // 运行时 Component（by name，慢路径）
   double temp = snt::components::get_float(e, "BronzeFurnaceState", "temperature");
   snt::components::set_float(e, "BronzeFurnaceState", "temperature", temp + 2.5);
   ```

#### 5.8.8 性能特性

| 特性 | 实现 |
|---|---|
| 数据连续布局 | EnTT sparse set，同类型 Component 内存连续 |
| Cache 友好 | System 遍历 view 时按 Component 类型遍历，命中率高 |
| 并行 tick | Worker System 可多线程跑（EnTT view 可分片迭代） |
| 批量处理 | 一次 tick 处理所有 MachineComponent，无虚函数调用 |
| 内存紧凑 | Component 是 POD struct，无虚函数表 |

#### 5.8.9 ECS + AngelScript 架构图

```text
┌──────────────────────────────────────────────────────────────┐
│ AngelScript (.as → JIT)                                       │
│  • L1: recipes / materials / quests (data)                   │
│  • L2: items / blocks / components (registration)            │
│  • L3: systems / events (behavior)                           │
├──────────────────────────────────────────────────────────────┤
│ Script Binding Layer (手动注册 C++ API → AS)                  │
│  • Component access (typed + runtime-reflected)              │
│  • System registration (callback-based)                      │
│  • Event dispatch (script_id + callback_id)                  │
├──────────────────────────────────────────────────────────────┤
│ ECS World (snt_engine/ecs/)                                  │
│  • Entity registry (entt::registry)                          │
│  • Compile-time Components (Position, Health, ...)           │
│  • Runtime Components (script-registered, sparse set)        │
│  • System scheduler (main + worker pool + async)             │
├──────────────────────────────────────────────────────────────┤
│ Engine Systems (C++, hot path)                               │
│  • MachineTickSystem / PowerNetworkSystem / etc.             │
│  • ChunkStreamingSystem / ChunkMeshingSystem                 │
│  • RenderSubmitSystem / PhysicsSystem                        │
├──────────────────────────────────────────────────────────────┤
│ Render Backend (Vulkan) + Platform (SDL3)                    │
└──────────────────────────────────────────────────────────────┘
```

## 6. 现有资产复用清单

### 6.1 可直接复用（剥离 godot-cpp 依赖）

| 模块 | 路径 | 复用方式 |
|---|---|---|
| 地形生成 | `src/core/simulation/*`、`gd_terrain_generator.cpp` | 算法保留，类型替换（PackedByteArray→std::vector） |
| Greedy mesh 算法 | `gd_chunk_helper.cpp::build_greedy_mesh` | 算法保留，输出改为自定义 VertexBuffer |
| 碰撞面生成 | `gd_chunk_helper.cpp::build_collision_faces` | 同上 |
| 区域保存 | `src/core/save/*` (region_file, chunk_serializer) | 文件格式保留，去 Variant |
| 配方/物品/材料 | `src/core/crafting/*`、`material/*`、`machine/*` | 数据结构保留，Manager 类重构为 ECS System |
| 网络 | `src/core/multiplayer/*`、SteamP2P | Steamworks 独立于 Godot |
| Quest 系统 | `src/core/quest/*` | 数据结构保留，Manager 改为 QuestRegistry |
| AE2 自动合成 | `src/core/ae2/*` | 算法保留，重构为 CraftingSystem |
| Multiblock | `src/core/multiblock/*` | 算法保留，重构为 MultiblockSystem |
| SFM 流程图 | `src/core/sfm/*` | 算法保留，重构为 SchedulingSystem |
| Power/Fluid/Item 网络 | `src/core/network/*` | 算法保留，重构为 NetworkSystem 系列 |
| Source Law 系统 | `src/core/source_law/*`、`magic/*` | 数据结构保留，重构为 SourceLawSystem |

### 6.1.1 ECS 重构映射

现有 Manager 类（OOP）→ ECS System 映射：

| 现有 GDScript/C++ Manager | 新 ECS System | 备注 |
|---|---|---|
| `FurnaceManager.gd` | `MachineTickSystem`（机器类型 = "furnace"） | 5 种机器统一为 1 个 System |
| `BloomeryManager.gd` | `MachineTickSystem`（机器类型 = "bloomery"） | |
| `PitKilnManager.gd` | `MachineTickSystem`（机器类型 = "pit_kiln"） | |
| `CharcoalPitManager.gd` | `MachineTickSystem`（机器类型 = "charcoal_pit"） | |
| `AnvilManager.gd` | `MachineTickSystem`（机器类型 = "anvil"） | |
| `ChunkRendererBridge.gd` | `ChunkStreamingSystem` + `ChunkMeshingSystem` + `RenderSubmitSystem` | 拆为 3 个 System |
| `MachineCollisionBridge.gd` | `CollisionUpdateSystem` | |
| `PlayerController.gd` | `PlayerMovementSystem` + `InputSystem` | 玩家也是 Entity |
| `FloatingOrigin.gd` | `FloatingOriginSystem` | |
| `UniverseManager.gd` | `DimensionSwitchSystem` | |
| `TickSystem` (现有 C++) | `SystemScheduler` | 已是 System 调度雏形 |

### 6.2 需要重写

| 模块 | 原因 |
|---|---|
| 所有 GDScript（30+） | 语言不同，改为 AngelScript |
| 所有 .tscn 场景 | 场景图没了，改为 JSON 布局 |
| ArrayMesh / MeshInstance3D | 改为 Vulkan VertexBuffer |
| CharacterBody3D / ConcavePolygonShape3D | 改为自研碰撞查询 |
| RenderingServer 调用 | 改为 Vulkan |
| Control 节点 UI | 改为 MUI 复刻 |
| ResourceLoader (.tres) | 改为 JSON + 二进制 |
| 现有 Manager 类（OOP） | 改为 ECS System（见 6.1.1） |
| 现有信号系统 | 改为 ECS 事件总线 |

### 6.3 要扔掉

| 模块 | 原因 |
|---|---|
| `FrameBudgetController` | 主线程不再被阻塞，不需要补锅 |
| `RuntimePerfMonitor` 的 GDScript 部分 | 改为 C++ profiler |
| `ChunkRendererBridge` 的 GDScript 部分 | 整个替换为 C++ ChunkRenderer |
| 所有 `@export` 字段配置 | 改为 JSON 配置 |
| `FloatingOrigin` 的 GDScript 部分 | 改为 C++ 实现 |
| `PhysicsTerrainSyncBridge` | 物理层重写 |

## 7. 目录结构

> **状态说明**：`✅` 已实现，`❌` 未实现（按阶段计划待建），`⚠️` 部分实现。
> 实际引擎根目录为 `snt_engine/`（设计初稿写作 `engine/`，落地时改名以避免与 `engine/engine/` 子模块混淆）。

```text
ScienceAndTheology/
├── snt_engine/                      # 自研引擎（设计初稿写作 engine/，落地改名）
│   ├── CMakeLists.txt               # ✅ 顶层 CMake，定义 snt_engine_settings 接口目标
│   ├── cmake/                       # ✅ 第三方下载脚本
│   │   ├── download_third_party.ps1
│   │   └── fetch_third_party.cmake  # FetchContent: entt/vma/stb/json/vulkan-headers/shaderc/angelscript
│   │
│   ├── engine_app/                  # ✅ 应用层
│   │   └── main.cpp                 #    （game_loop/scene_manager/save_entry 未实现，由 Engine 类承担）
│   │
│   ├── engine/                      # ✅ 引擎层 — 编排器
│   │   ├── engine.cpp/.h            #    Engine 类：init / main_loop / shutdown
│   │   └── CMakeLists.txt
│   │
│   ├── core/                        # ✅ 核心工具层（设计初稿未单列，实际抽出）
│   │   ├── log.cpp/.h               #    自研 Logger（Trace/Debug/Info/Warn/Error/Fatal + 模块通道）
│   │   ├── snt_assert.h             #    SNT_ASSERT / SNT_VERIFY（注意：不能命名为 assert.h，会遮蔽 UCRT）
│   │   ├── expected.h               #    Expected<T, Error>（替代 bool 返回）
│   │   ├── error.h                  #    Error 对象（code/message + with_context）
│   │   ├── binary_reader.h          #    BinaryReader（二进制反序列化）
│   │   ├── binary_writer.h          #    BinaryWriter（二进制序列化）
│   │   ├── serializer.h             #    Serializer<T> 模板特化
│   │   ├── uuid.cpp/.h              #    UUID（EntityGuid 底层）
│   │   ├── hash.h                   #    编译期 hash
│   │   ├── clock.h                  #    高精度时钟
│   │   ├── events.h                 #    事件类型定义
│   │   ├── profiling.h              #    性能剖析宏
│   │   ├── engine_config.cpp/.h     #    engine.json 配置加载
│   │   ├── path_utils.cpp/.h        #    引擎根路径解析（基于 fetch_third_party.cmake 标记）
│   │   ├── memory_tracker.cpp/.h    #    内存追踪
│   │   └── job_system.cpp/.h        #    ✅ Job System（Counter-based + work-stealing + parallel_for）
│   │                                #       P2 SystemScheduler 的底层；本身不依赖 ECS
│   │
│   ├── ecs/                         # ✅ ECS 核心 (EnTT) — 扁平化，不再嵌套在 engine/engine/ 下
│   │   ├── entt_config.h            #    EnTT 统一 include + 断言路由到 SNT_LOG_FATAL
│   │   ├── entity_guid.cpp/.h       #    稳定 EntityGuid（serialization 用）
│   │   ├── world.cpp/.h             #    World：entt::registry 包装 + Guid<->entity 反查表
│   │   ├── system.h                 #    System 基类（update(World&, dt)）
│   │   ├── components.h             #    ⚠️ 仅 Transform/MeshRef/Camera；缺 Position/Velocity/Health
│   │   ├── event_bus.h              #    事件总线
│   │   ├── camera_system.cpp/.h     #    CameraSystem（读 InputSystem 状态）
│   │   ├── system_scheduler.cpp/.h  #    ❌ 未实现（P2 待建，基于 job_system.h 包装）
│   │   └── systems/                 #    ❌ 未实现（P3+ 标准 System 实现目录）
│   │       ├── machine_tick_system.cpp        # ❌ P7
│   │       ├── chunk_streaming_system.cpp     # ❌ P3
│   │       ├── chunk_meshing_system.cpp       # ❌ P3
│   │       ├── render_submit_system.cpp       # ❌ P3
│   │       ├── player_movement_system.cpp     # ❌ P4
│   │       ├── power_network_system.cpp       # ❌ P7
│   │       ├── fluid_network_system.cpp       # ❌ P7
│   │       └── ...
│   │
│   ├── input/                       # ✅ 输入层（从 platform 拆出，独立模块）
│   │   ├── input_state.h
│   │   └── input_system.cpp/.h
│   │
│   ├── render/                      # ✅ 渲染系统层（ECS System 入口）
│   │   └── render_system.cpp/.h     #    RenderSystem：遍历 View<Transform, MeshRef> 提交绘制
│   │
│   ├── renderer/                    # ✅ RenderGraph 层（设计初稿未单列，实际抽出）
│   │   ├── render_graph.cpp/.h      #    渲染Pass管理 + 资源依赖追踪
│   │   ├── render_graph_pass.cpp/.h
│   │   ├── render_graph_resource.cpp/.h
│   │   ├── pipeline_cache.cpp/.h    #    Pipeline 缓存
│   │   └── transient_pool.cpp/.h    #    Transient 资源池
│   │
│   ├── render_backend/              # ✅ Vulkan 后端
│   │   ├── vulkan_instance.cpp/.h
│   │   ├── vulkan_device.cpp/.h
│   │   ├── vulkan_swapchain.cpp/.h
│   │   ├── vulkan_frame.cpp/.h      #    per-frame 同步 + command buffer
│   │   ├── vulkan_pipeline.cpp/.h
│   │   ├── vulkan_descriptor.cpp/.h
│   │   ├── vulkan_buffer.cpp/.h
│   │   ├── vulkan_depth.cpp/.h
│   │   ├── vulkan_mesh.cpp/.h
│   │   ├── command_context.cpp/.h
│   │   └── vma_impl.cpp             #    VMA 实现翻译单元
│   │
│   ├── assets/                      # ✅ 资产系统（P5 内容提前实现）
│   │   ├── asset_handle.h           #    MeshHandle 等句柄类型
│   │   ├── asset_cache.h            #    资产缓存
│   │   ├── asset_manifest.cpp/.h    #    JSON manifest（稳定 handle 分配）
│   │   ├── asset_manager.cpp/.h     #    资产管理器单例
│   │   ├── mesh_asset_loader.cpp/.h #    Mesh 加载器
│   │   ├── texture_cache.cpp/.h     #    ❌ 未实现（P5）
│   │   ├── font_atlas.cpp/.h        #    ❌ 未实现（P5/P6）
│   │   └── shader_cache.cpp/.h      #    ❌ 未实现（P5，shaderc 集成）
│   │
│   ├── scene/                       # ✅ 场景序列化（Phase 4，二进制 .bin 格式）
│   │   └── scene.h                  #    save_scene<MeshT> / load_scene<MeshT> 模板
│   │
│   ├── script/                      # ✅ AngelScript 系统（P7 内容提前实现骨架）
│   │   ├── script_engine.cpp/.h     #    asIScriptEngine 封装
│   │   ├── script_context.cpp/.h    #    执行上下文
│   │   ├── script_module.cpp/.h     #    模块管理
│   │   ├── script_loader.cpp/.h     #    加载 + .as mtime 热重载
│   │   ├── script_manager.cpp/.h    #    顶层管理器
│   │   ├── file_watcher.cpp/.h      #    ❌ 未实现（P7）
│   │   ├── registry_hub.cpp/.h      #    ❌ 未实现（P7：RecipeRegistry/EventRegistry）
│   │   └── api/                     #    ❌ 未实现（P7：暴露给脚本的 API 头）
│   │       ├── api.h
│   │       ├── events.h
│   │       └── macros.h
│   │
│   ├── data/                        # ❌ 未实现（P2 数据核心，从 src/core 移植）
│   │   ├── world_data.cpp/.h        #    源：src/bindings/world/gd_world_data.cpp
│   │   ├── terrain_generator.cpp/.h #    源：src/core/world_gen/terrain_generator.cpp
│   │   ├── save_manager.cpp/.h      #    源：src/core/save/save_manager.cpp
│   │   ├── tick_system.cpp/.h       #    源：src/core/simulation/*
│   │   └── networks/                #    power/fluid/item/signal
│   │
│   ├── ui/                          # ❌ 未实现（P6 MUI 复刻）
│   │   ├── view/                    #    View 系统（保留模式）
│   │   ├── viewmodel/               #    ViewModel + 数据绑定
│   │   ├── text/                    #    文本引擎 (HarfBuzz + ICU + SDF)
│   │   ├── animation/               #    动画系统
│   │   ├── render/                  #    Arc 2D 渲染器
│   │   └── assets/                  #    字体 / 布局 JSON
│   │
│   ├── voxel/                       # ❌ 未实现（P3 Voxel 渲染）
│   │   ├── chunk_renderer.cpp/.h
│   │   ├── greedy_mesh.cpp/.h       #    从 gd_chunk_helper 移植
│   │   ├── collision_faces.cpp/.h
│   │   ├── lod_manager.cpp/.h
│   │   └── vertex_buffer_pool.cpp/.h
│   │
│   ├── physics/                     # ❌ 未实现（P4 Voxel 物理）
│   │   ├── voxel_collision.cpp/.h
│   │   ├── player_controller.cpp/.h
│   │   └── ray_cast.cpp/.h
│   │
│   ├── audio/                       # ❌ 未实现（miniaudio 封装）
│   ├── network/                     # ❌ 未实现（SteamP2P 复用）
│   │
│   ├── platform/                    # ✅ SDL3 平台层
│   │   └── window.cpp/.h            #    Window + create_vulkan_surface()；input 已拆到 input/
│   │
│   ├── shaders/                     # ✅ GLSL 源码（mesh.frag/vert + triangle.frag/vert）
│   ├── tools/                       # ✅ 独立开发工具
│   │   └── gen_default_scene.cpp    #    一次性场景生成器（产出 game/scenes/default_scene.bin）
│   ├── tests/                       # ✅ 单元测试（107 通过）
│   │   ├── test_main.cpp
│   │   ├── test_entity_guid.cpp
│   │   ├── test_asset_manifest.cpp
│   │   ├── test_asset_cache.cpp
│   │   ├── test_binary_io.cpp
│   │   ├── test_scene_serialize.cpp
│   │   ├── test_job_system.cpp
│   │   ├── test_script_engine.cpp
│   │   ├── test_script_loader.cpp
│   │   ├── test_engine_config.cpp
│   │   ├── test_path_utils.cpp
│   │   ├── test_clock.cpp
│   │   ├── test_hash.cpp
│   │   ├── test_uuid.cpp
│   │   ├── test_data.cpp            # ❌ 未实现（P2：chunk (0,0,0) non-air cell 数）
│   │   └── test_ecs.cpp             # ❌ 未实现（P2：1000 Entity + Component + System 耗时）
│   └── third_party/                 # ✅ 第三方库
│       ├── SDL-release-3.4.10/      #    SDL3 源码（.gitignore 忽略）
│       ├── _downloads/              #    FetchContent 下载缓存（.gitignore 忽略）
│       └── CMakeLists.txt           #    聚合 entt/vma/stb/json/vulkan-headers/shaderc/angelscript
│
├── game/                            # ✅ 运行时资源（由 Engine 加载）
│   ├── config/
│   │   ├── engine.json              #    引擎配置（窗口/Vulkan/scene 路径）
│   │   └── default_manifest.json    #    资产 manifest
│   └── scenes/
│       └── default_scene.bin        #    默认场景（camera + 2 cubes，二进制序列化）
│
├── scripts/                         # ❌ 未迁移（旧 GDScript；P7 改写为 AngelScript .as）
│   └── ...                          #    详见 项目架构总览.md
│
├── resource/                        # 旧 Godot 资源（迁移后清理）
├── src/                             # 旧 C++ 代码（P2 数据移植源，迁移完毕后删除）
│   └── core/
│       ├── world_gen/               #    terrain_generator 源
│       ├── save/                    #    save_manager 源
│       └── simulation/              #    tick_system 源
├── docs/
└── CMakeLists.txt
```

### 7.1 模块依赖关系（实际）

CMake 顶层定义的模块依赖链（自上而下，禁止反向）：

```text
engine_app → engine → {render, script, scene, ui, ecs, assets, renderer}
                    → {input, platform, core}
renderer → render_backend → platform
ecs → {input, core}
assets → render_backend
scene → {ecs, assets, core}
script → core
render → {ecs, renderer, assets}
data → core     # ❌ 未实现
```

### 7.2 与设计初稿的主要偏差

| 设计初稿 | 实际落地 | 原因 |
|---|---|---|
| `engine/` 顶级目录 | `snt_engine/` | 避免与 `engine/engine/` 子模块命名混淆 |
| `engine/engine/ecs/` 嵌套两层 | `snt_engine/ecs/` 扁平 | 模块独立成静态库，无需嵌套 |
| `engine/engine/ecs/types.h` | 直接用 `entt::entity` | EnTT 已提供类型，无需包装 |
| 未规划 `core/` 模块 | `snt_engine/core/` 独立模块 | log/path_utils/job_system/serializer 等被多模块共用 |
| 未规划 `renderer/` 模块 | `snt_engine/renderer/` RenderGraph | P1.5 提前实现 RenderGraph 资源依赖追踪 |
| 未规划 `scene/` 模块 | `snt_engine/scene/` 二进制序列化 | Phase 4 实现场景存档 |
| 未规划 `input/` 模块 | `snt_engine/input/` 独立 | InputSystem 与 CameraSystem 解耦 |
| 未规划 `assets/` 在 P1 阶段 | `snt_engine/assets/` 已实现 | P5 内容提前到 Phase 2（场景加载需要 MeshHandle） |
| 未规划 `script/` 在 P1 阶段 | `snt_engine/script/` 骨架已实现 | P7 内容提前验证 AngelScript 集成 |
| `engine/engine/data/` | `snt_engine/data/`（待建） | 扁平化 |

## 8. 阶段计划

每个阶段结束后游戏必须能跑（哪怕只是空窗口）。

### P0 — 架构基线（本文档）

**目标**：共识基线
**产出**：本文档
**验证**：文档评审通过

### P1 — 骨架搭建

**目标**：SDL3 窗口 + Vulkan 三角形 + ImGui 调试层
**产出**：
- `engine/CMakeLists.txt` 骨架
- `engine/platform/window.cpp` SDL3 窗口
- `engine/render_backend/vulkan_device.cpp` Vulkan 初始化
- `engine/engine_app/main.cpp` 主循环
- 一个三角形能画出来
**验证**：运行可执行文件，看到 Vulkan 三角形

### P2 — 数据核心移植 + ECS 骨架

**目标**：把 `GDWorldData` + `TerrainGenerator` 剥离 godot-cpp，移到 `snt_engine/data/`；同时搭建 ECS 骨架
**完成度**：约 30%（ECS 骨架 + Job System 已就位；数据移植 / SystemScheduler / 标准 Component / P2 测试未动）

**产出**：
- ❌ `world_data.cpp` 用 std::vector 替换 PackedByteArray
      （源：`src/bindings/world/gd_world_data.cpp`；目标 `snt_engine/data/` 未建）
- ❌ `terrain_generator.cpp` 算法保留，去 Variant
      （源：`src/core/world_gen/terrain_generator.cpp`）
- ❌ `save_manager.cpp` region_file 格式保留
      （源：`src/core/save/save_manager.cpp`）
- ✅ `snt_engine/ecs/` 骨架：World / Entity / Component / System 接口（基于 EnTT）
      （实际位置：`snt_engine/ecs/`；World 包装 entt::registry，EntityGuid 稳定 ID，System 基类已就位）
- ❌ `SystemScheduler` 主线程 + worker pool 调度框架
      （底层 Job System 已实现：`snt_engine/core/job_system.h` — Counter-based + work-stealing + parallel_for；尚需包装为 SystemScheduler）
- ⚠️ 标准 Component 定义（Position/Velocity/Health 等）
      （现有：Transform / MeshRef / Camera，见 `snt_engine/ecs/components.h`；缺 Position / Velocity / Health 等）
- ❌ 单元测试：能生成一个 chunk 的 terrain 数据；能创建 Entity + 加 Component + 跑一个空 System
      （待建：`snt_engine/tests/test_data.cpp` + `test_ecs.cpp`）

**验证**：
- ❌ `./engine_test data_test` 输出 chunk (0,0,0) 的 non-air cell 数
- ❌ `./engine_test ecs_test` 创建 1000 个 Entity + Component，跑 System，输出耗时

**P2 剩余工作建议顺序**：
1. 创建 `snt_engine/data/` 模块 + CMakeLists.txt（依赖 core）
2. 移植 `world_data.cpp`（std::vector 替换 PackedByteArray，去 godot-cpp）
3. 移植 `terrain_generator.cpp`（去 Variant，复用算法）
4. 移植 `save_manager.cpp`（region_file 格式保留）
5. 在 `components.h` 补 Position / Velocity / Health 等标准 Component
6. 实现 `SystemScheduler`（包装 Job System，主线程 60Hz + worker pool 20Hz/10Hz + async）
7. 写 `test_data.cpp` + `test_ecs.cpp` 满足验证条件

### P3 — Voxel 渲染

**目标**：自研 `ChunkRenderer`，能渲染一个 chunk
**产出**：
- `greedy_mesh.cpp` 从 `gd_chunk_helper` 移植
- `collision_faces.cpp` 同上
- `chunk_renderer.cpp` Vulkan vertex buffer + draw call
- `vertex_buffer_pool.cpp` 内存池
- 主循环能加载 chunk 数据并渲染
**验证**：运行看到地面 chunk（哪怕只是纯色）

### P4 — 玩家控制 + 物理

**目标**：SDL 输入 + 自研碰撞 + 第一人称相机
**产出**：
- `voxel_collision.cpp` AABB vs voxel grid
- `player_controller.cpp` 移动 / 跳跃 / 重力
- `ray_cast.cpp` DDA 算法
- 鼠标锁定 + 视角控制
**验证**：能走动看世界，能挖方块

### P5 — 资产管线 + 贴图

**目标**：贴图加载 + 材质 atlas + 完整方块渲染
**产出**：
- `loader.cpp` JSON + 二进制
- `texture_cache.cpp` stb_image
- `font_atlas.cpp` FreeType
- `shader_cache.cpp` shaderc
- 方块贴图能正确显示
**验证**：能看到不同材质的方块

### P6 — UI 系统（MUI 复刻）

**目标**：完整复刻 MUI 全部能力（见 5.3.2）
**产出**：
- View 系统：ViewGroup + 基础控件 + 布局三阶段
- ViewModel + 数据绑定
- 文本引擎：HarfBuzz + ICU + SDF + Emoji
- Arc 2D 渲染器
- 动画系统
- 至少实现 hotbar / inventory / crafting 三个 UI
**验证**：能打开背包、合成、看到中文 + Emoji 正确渲染

### P7 — 玩法迁移（AngelScript + ECS System）

**目标**：机器、quest、save、网络全部迁移为 AngelScript + ECS System
**产出**：
- `ScriptManager` + `DynamicLibrary` + `FileWatcher` + `RegistryHub`
- 5 个机器系统重构为 `MachineTickSystem`（机器类型区分 furnace/bloomery/pit_kiln/charcoal_pit/anvil）
- `QuestRegistry` + quest 注册 API
- `SaveManager` C++ 实现（region_file 复用）
- SteamP2P 网络层剥离 Godot
- 至少 3 个示例脚本 DLL：`furnace_recipes.dll` / `bronze_furnace.dll` / `basic_quests.dll`
- `/snt reload` 命令可用
**验证**：能烧矿、能接任务、能存档、能联机；改 .as 后 `/snt reload` 立即生效

## 9. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| Vulkan 学习曲线陡 | P1/P3 延期 | 参考 vkguide.dev；先用 vulkan-tutorial 跑通三角形 |
| MUI 复刻工作量巨大 | P6 延期 | 分子阶段：先 View+ViewModel，再文本引擎，再动画 |
| ECS 学习曲线 + 重构成本 | P2/P7 延期 | EnTT 文档完善；先骨架后迁移；Manager→System 是机械映射 |
| DLL 热重载跨平台坑 | P7 延期 | 5 个坑已有对应方案（见 5.5.4）；先用 Windows 验证 |
| 跨平台坑（macOS MoltenVK） | macOS 不工作 | 优先 Win/Linux，macOS 后期处理 |
| 现有 C++ 剥离 godot-cpp 工作量大 | P2 延期 | 渐进式：先保留 godot-cpp 编译，逐步替换 |
| 资产格式迁移 | 旧存档失效 | 写 .tres → JSON 转换器；旧存档一次性迁移 |
| 网络协议变更 | 旧客户端不兼容 | 协议版本号；不向后兼容，强制升级 |
| 脚本编译等待 | 开发体验 | AngelScript JIT 编译 <50ms；file watcher 自动 reload |

## 10. 不做什么（边界）

- **不做编辑器**：调试用 ImGui，不做 Godot 那样的可视化编辑器
- **不做跨平台移动端**：Windows/Linux/macOS 优先，Android/iOS 后期再说
- **不做 VR/AR**：当前不考虑
- **不做向后兼容**：旧存档一次性迁移，之后不再保证
- **不做 mod API**：先把核心玩法跑通，mod API 后期再开
- **不引入重型第三方引擎**：不依赖 Unreal/Bevy/O3DE
- **不做光线追踪**：光栅化优先，RT 后期可选

## 11. 后续待细化（每阶段开始前补）

- P1：Vulkan 设备选择策略 / VMA 配置
- P2：WorldData 内存布局 / 线程模型 / ECS Component 池大小 / System 调度优先级
- P3：meshing worker 线程池大小 / staging buffer 策略
- P4：玩家碰撞精度 / 物理步长
- P5：贴图压缩格式 / atlas 大小上限
- P6：MUI 复刻子阶段拆分（View / Text / Animation / DataBinding）
- P7：SteamP2P 协议迁移 / Replication 策略 / AS reload 状态管理细节

---

**本文档版本**：v1.1
**最后更新**：2026-07-06
**作者**：架构共识基线
**变更记录**：
- v1.0 (2026-07-06)：初版，方案 A + Lua+sol2
- v1.2 (2026-07-08)：脚本系统从 C++NativeScript DLL 改为 AngelScript（C++ 语法、JIT 编译、<50ms 热重载）；放弃 DLL 方案因迭代慢 + ABI 约束
- v1.1 (2026-07-06)：脚本系统改为 C++NativeScript（参考 GroovyScript）；新增 ECS 架构（基于 EnTT）；Manager 类重构为 System
