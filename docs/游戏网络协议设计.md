# ScienceAndTheology 游戏网络协议设计

> 当前基线，更新于 2026-07-14。本文描述 `game/network/` 的游戏协议边界；可复用 socket/Steam transport 以 [自研引擎架构设计.md](自研引擎架构设计.md) 为准。

## 1. 目标和所有权

`snt_network` 只负责传输可靠性、TCP/UDP 关联、wire framing 和 fixed-tick phase。它不知道玩家、方块、任务、配方、登录凭据或 AOI。

`snt_game_network` 负责最新版本的游戏 payload、玩家准入和后续玩法同步契约。它保持无头闭包，只依赖 `snt_core`、`snt_network`、`snt_ecs` 和 `snt_voxel_data`，不链接 SDL、Vulkan、Steamworks 或 client runtime。

| 层 | 格式 | 版本策略 | 责任 |
| --- | --- | --- | --- |
| 引擎 transport | `SNTR` 20-byte wire header | `kCurrentReplicationProtocolVersion` | frame 长度、通道、server tick、TCP/UDP/Steam adapter |
| 游戏 payload | `SNTG` 12-byte envelope | `kCurrentGameReplicationProtocolVersion` | 游戏消息类型、登录与玩法语义 |

两个版本独立拒绝不匹配值，不提供旧协议探测、转换或兼容 wrapper。

## 2. 当前 envelope

`SNTG` 使用 network byte order：

```text
magic:u32 | game_protocol_version:u16 | kind:u8 | reserved:u8 | payload_length:u32 | payload
```

`reserved` 必须为零，payload length 必须与实际字节完全匹配。所有当前声明的游戏消息必须走 reliable ordered channel；要添加 UDP gameplay 消息，必须先新增 kind 并在 `validate_game_replication_channel` 中显式登记。

| 方向 | 已声明消息 | 当前实现 |
| --- | --- | --- |
| client -> server | `LoginRequest` | 已实现 typed codec 和认证状态转换 |
| client -> server | `Command` | 已实现 typed codec；认证后转交 `IGameReplicationCommandSink` |
| client -> server | `InterestUpdate`、`SnapshotAcknowledgement` | 仅声明，当前明确拒绝 |
| server -> client | `LoginAccepted` | 已实现，scheduler barrier 后发送 |
| server -> client | `Snapshot`、`Delta`、`Notice` | 仅声明，当前未产生 payload |

登录名最大 48 UTF-8 bytes，稳定 player id 最大 128 bytes，凭据最多 1024 bytes，命令 payload 最多 64 KiB。凭据是 opaque bytes，日志不得记录它或其派生值。

## 3. 服务端时序

```text
transport poll
  -> GameServerReplicationHandler inbound validation/authentication/command intake
  -> game fixed tick
  -> SystemScheduler barrier
  -> GameServerReplicationHandler outbound login/snapshot/delta emission
```

认证通过后，`LoginAccepted` 被排入同一 tick 的 outbound 队列，因此不会在 scheduler barrier 前发送“已进入世界”的假状态。命令入口只在 simulation main thread 调用，具体实现必须将后续 World 变更排进确定性的游戏 command 边界。

当前 `ScienceAndTheologyServerSession` 安装 `ClosedGamePeerAuthenticator`。因此显式 `--network` 可以验证 TCP+UDP transport 和协议拒绝路径，但不会无意中开放匿名玩家准入。登录尝试会带明确 protocol error 断开。

## 4. 预留接口

以下接口已声明在 `game/network/game_replication_services.h`，但除 handler 使用认证器和命令 sink 外，其余均没有生产实现：

- `IGamePeerAuthenticator`：把凭据验证和持久玩家身份映射留在游戏层。
- `IGameReplicationCommandSink`：认证后按 client sequence 接收命令，并在 simulation main thread 排队确定性 mutation。
- `IGameReplicationInterestProvider`：以当前 `ChunkKey`、稳定 `EntityGuid` 和机器实体集合计算 AOI。
- `IGameReplicationSnapshotSource`：按每 peer 的 chunk/entity/block budget 产出 server-originated snapshot 或 delta。

`GameReplicationInterest` 目前使用现有 `ChunkKey`。统一宇宙迁移到 Sector 坐标时，直接更新该最新接口和协议版本，不保留 ChunkKey 到 Sector 的兼容桥。

## 5. 认证取舍

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| 将共享口令硬编码到 server config | 最快，LAN 调试简单 | 秘密管理和明文配置会成为长期 API，难以迁移 |
| 直接绑定 Steam 身份 | 平台接入快 | dedicated server 和非 Steam 调试被锁定 |
| 可注入 `IGamePeerAuthenticator` + 默认关闭准入（已选择） | 不绑定账号平台；可分别接入 LAN、Steam 或账号服务；默认不误开放世界 | 首次只能完成协议和测试替身，真实玩家登录还需选择并实现 provider |

选择第三种与 `snt_network` 的 Steam SDK-neutral adapter 一致。认证平台尚未决定，因此不把任一 provider 伪装为当前实现。

## 6. 下一步

1. 选定并实现一个 game-owned `IGamePeerAuthenticator`，由 server package 显式注入，而不是修改引擎 transport。
2. 定义最新的 game command ids，并实现 `IGameReplicationCommandSink` 到确定性 World command 的映射。
3. 实现 AOI、snapshot 和 delta codec，同时将预算执行接入 `IGameReplicationInterestProvider` / `IGameReplicationSnapshotSource`。
4. 为 client host 增加连接会话和协议消费；当前 graphical client 没有联机连接层。

验证基线：`GameReplicationProtocolTest` 和 `GameReplicationHandlerTest` 覆盖 envelope 拒绝、可靠通道、登录确认、未认证命令拒绝和断开清理；`snt_game_server --ticks 3` 覆盖无头宿主启动与关闭。
